# Backend Nest API (always used by the web app — never the UI host).
VITE_API_URL=http://localhost:8000
VITE_SOCKET_URL=http://localhost:8000
# Esri token for satellite imagery tiles (or set ESRI_TOKEN in ecofleet-backend/.env)
# VITE_ARCGIS_TOKEN=
